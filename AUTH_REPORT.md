# تقرير تنفيذ: ربط نظام المصادقة (Authentication) — تطبيق مغسلتي

**التاريخ:** 22 سبتمبر 2026
**النطاق:** إنشاء الحساب، تفعيل رقم الهاتف، تسجيل الدخول، تجديد الجلسة
**الحالة:** مكتمل ومُختبر على السيرفر الفعلي

---

## 1. ملخص تنفيذي

تم ربط دورة المصادقة كاملة بالـ API الخاص بالمشروع (`lavanderia.runasp.net`) بدلاً من الشاشات الثابتة (static) التي كانت موجودة.

المستخدم الآن يقدر ينشئ حساب، يفعّل رقمه بكود التحقق، ويدخل التطبيق — وتُحفظ جلسته تلقائياً فلا يحتاج لتسجيل الدخول في كل مرة.

**أثناء التنفيذ تم اكتشاف وإصلاح 5 أعطال جوهرية** كانت ستمنع التطبيق من العمل مع السيرفر أساساً (التفاصيل في القسم 4).

---

## 2. المسار الوظيفي بعد التنفيذ

```
إنشاء حساب  →  رسالة تأكيد  →  شاشة كود التحقق  →  رسالة تأكيد  →  الصفحة الرئيسية
تسجيل دخول  →  رسالة تأكيد  →  الصفحة الرئيسية
```

**ملاحظات على التجربة:**

- بعد تفعيل رقم الهاتف، المستخدم يدخل التطبيق مباشرة — لا يُطلب منه تسجيل الدخول مرة أخرى، لأن الجلسة تُفتح تلقائياً عند التفعيل.
- رسائل النجاح والخطأ تُعرض كما يرسلها السيرفر بالعربية (مثل: "تم إنشاء الحساب بنجاح. يرجى تفعيل رقم الهاتف.").
- قائمة المدن تُحمّل من السيرفر عند فتح شاشة التسجيل (72 مدينة).
- المستخدم يقدر يحدد موقعه بضغطة واحدة، ويعدّل العنوان يدوياً بعدها.

---

## 3. نقاط الـ API المرتبطة

| الوظيفة | نقطة الاتصال | الحالة |
|---|---|---|
| قائمة المدن | `GET /api/auth/cities` | مُختبر ✓ |
| إنشاء حساب | `POST /api/auth/customer/register` | مُختبر ✓ |
| تفعيل رقم الهاتف | `POST /api/auth/customer/verify-phone` | مُختبر ✓ |
| تسجيل الدخول | `POST /api/auth/customer/login` | مُختبر ✓ |
| تجديد الجلسة | `POST /api/auth/refresh-token` | مُختبر ✓ |

جميع النقاط تم اختبارها فعلياً على السيرفر وليس نظرياً.

---

## 4. أعطال تم اكتشافها وإصلاحها

هذه أعطال كانت موجودة في المشروع وظهرت أثناء الربط. لولا إصلاحها لما عمل النظام.

### 4.1 عنوان السيرفر يشير لمشروع آخر
كان عنوان الـ API الأساسي يشير إلى مشروع `professional-lawyer` غير المرتبط بالتطبيق. تم تصحيحه إلى سيرفر مغسلتي.

### 4.2 تجديد الجلسة معطّل بالكامل
مسار تجديد الجلسة كان خاطئاً (`Auth/refresh-token` بدلاً من `api/auth/refresh-token`)، كما أن قراءة البيانات كانت من مكان خاطئ في رد السيرفر.

**الأثر:** كان المستخدم سيُطرد من التطبيق ويُطلب منه تسجيل الدخول من جديد كلما انتهت صلاحية جلسته — رغم أن النقطة تعمل بشكل سليم على السيرفر.

### 4.3 خدمات التطبيق غير مُفعّلة
منظومة ربط الخدمات (Dependency Injection) لم تكن مُستدعاة، وأحد المكوّنات الأساسية لم يكن مُسجّلاً.

**الأثر:** كان التطبيق سيتوقف (crash) فور الضغط على زر التسجيل.

### 4.4 رسالة خطأ مضللة عند خطأ كلمة المرور
عند إدخال كلمة مرور خاطئة، كان التطبيق يعرض "عاود التسجيل من فضلك" ويمسح بيانات الجلسة — بدلاً من إخبار المستخدم أن البيانات غير صحيحة.

تم فصل الحالتين: خطأ في البيانات ≠ انتهاء الجلسة.

### 4.5 خانات كود التحقق غير مطابقة
حقل إدخال الكود كان مُثبتاً على 5 خانات، بينما السيرفر يرسل كوداً من 6 أرقام.

**الأثر:** كان يستحيل إدخال الكود الصحيح.

### 4.6 عدم تطابق أسماء وصيغ البيانات
- اسم حقل رقم الهاتف كان مختلفاً عمّا يتوقعه السيرفر.
- معرّف المستخدم يرسله السيرفر كنص، وكان التطبيق يتعامل معه كرقم — وهو ما كان سيسبب توقف التطبيق.
- البريد الإلكتروني قد يأتي فارغاً من السيرفر، وتمت معالجة هذه الحالة.

---

## 5. تنظيم الملفات

اتُّبع نفس النمط المعماري القائم في المشروع، وكل خاصية في مجلدها المستقل:

```
features/auth/
├── models/          ← نموذج بيانات مشترك بين تسجيل الدخول والتفعيل
├── register/        ← models · data · logic · view
├── otp/             ← data · logic · view
└── login/           ← data · logic · view
```

**ملاحظة على إعادة التنظيم:** منطق شاشة كود التحقق كان في البداية داخل مجلد التسجيل، وتم نقله إلى مجلده المستقل بناءً على توجيه فريق التطوير.

**نقطة تستحق الذكر:** تسجيل الدخول والتفعيل يرجعان نفس البيانات تماماً من السيرفر، لذا تمت مشاركة نموذج بيانات واحد بينهما بدلاً من تكرار الكود.

---

## 6. إدارة الجلسة

- بيانات الجلسة تُحفظ تلقائياً عند تسجيل الدخول أو تفعيل الرقم.
- اسم المستخدم وبياناته تُحفظ محلياً، فتستطيع أي شاشة عرضها دون طلب إضافي من السيرفر.
- عند انتهاء الجلسة نهائياً، تُمسح البيانات بالكامل.

---

## 7. الاختبار والتحقق

- **فحص الكود:** لا توجد أخطاء.
- **بناء التطبيق:** ناجح.
- **اختبار فعلي على السيرفر:** تم إنشاء حساب حقيقي، تفعيله، وتسجيل الدخول به، والتحقق من تجديد الجلسة.

---

## 8. بنود مفتوحة

| البند | الوضع | الملاحظة |
|---|---|---|
| اختيار الموقع من خريطة | مُنفَّذ بديلاً | التحديد يتم عبر GPS وتعديل العنوان يدوياً. الخريطة التفاعلية تتطلب إضافة مكتبة خرائط ومفاتيح تشغيل لنظامي أندرويد و iOS. |
| معلومات الجهاز | تُرسل فارغة | بناءً على توجيه فريق التطوير. تفعيلها لاحقاً يتطلب إضافة مكتبة مخصصة. |
| تسجيل الخروج | غير مرتبط | النقطة متاحة على السيرفر ومُعرّفة في التطبيق، لكن ربطها خارج نطاق هذه المهمة. |
| إعادة إرسال الكود | غير مرتبط | الزر يعمل كمؤقت زمني فقط حالياً. |
| نسيت كلمة المرور | غير مرتبط | خارج نطاق هذه المهمة. |

---

## 9. الخلاصة

دورة المصادقة تعمل بالكامل ومُختبرة على السيرفر الفعلي. تم بناؤها على نفس النمط المعماري المتبع في المشروع، مما يسهّل ربط بقية الخصائص لاحقاً بنفس الطريقة.

الأعطال المذكورة في القسم 4 كانت ستظهر عند ربط أي خاصية أخرى بالسيرفر، وإصلاحها الآن يفيد بقية أجزاء التطبيق.
